#DATABASE_URL=mysql+pymysql://devanshi:devanshi%40123@3.7.255.145:3306/vowconsole3
#DATABASE_URL=mysql+pymysql://Tarun:db_tarunji!123@3.7.255.145:3306/vowconsole3
DATABASE_USER=Tarun
DATABASE_PASSWORD=db_tarunji!123
DATABASE_HOST=45.58.59.119
DATABASE_PORT=3306
DATABASE_DEFAULT=maindata
ENV=devlopment
# ENV=production
# Automated hourly bio-attendance pipeline (src/hrms/bio_att_auto_pipeline.py),
# hosted by the standalone scheduler service (src/scheduler_main.py):
#   uvicorn src.scheduler_main:app --host 0.0.0.0 --port 48480
# Builds ONLY bio_attendance_basic + bio_attendance_process (no daily_attendance).
BIO_ATT_AUTO_ENABLED=false
BIO_ATT_AUTO_TENANT=sls
BIO_ATT_AUTO_BRANCH=4
BIO_ATT_AUTO_COMPANY_ID=2
BIO_ATT_AUTO_INTERVAL_MIN=60
# Schema (same MySQL server) holding the raw device feed table `punch_logs`
# (user_id = bio device id). Synced into bio_attendance_table before each run.
BIO_PUNCH_LOGS_DB=sls
